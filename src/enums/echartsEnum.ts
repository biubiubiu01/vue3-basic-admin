export enum EChartsEnum {
    LINE = "line",
    BAR = "bar",
    PIE = "pie",
    WORDCLOUD = "wordcloud",
    RADAR = "radar",
    LIQUIDfILL = "liquidfill",
    GRAPH = "graph",
    MAP = "map",
    SCATTER = "scatter",
    HEATMAP = "heatmap"
}

export type EChartsType = `${EChartsEnum}`;
